interface:
  display_name: "Bicep AVM Code"
  short_description: "Author AVM Bicep with environment params"
  default_prompt: "Use $bicep-avm-code to author or refactor AVM-first Bicep, generate environment parameter files when real values are known, and run diagnostics, build, lint, parameter builds, and the AVM posture checker."

dependencies:
  tools:
    - type: "mcp"
      value: "bicep"
      description: "Azure Bicep MCP server"
